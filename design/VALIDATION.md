# 原型验证记录

日期：2026-09-15。

## 已验证

- `node --check design/prototype.js`：通过。
- Codex 内置浏览器：八个画板均可切换并显示内容，默认桌面视口下手机内容区域没有横向溢出。
- 首页截图目视检查：导航、提醒、筛选、分组清单、固定新增按钮及底部导航显示正常。
- 快速新增：保存虚构事项后可连续录入，切换到明天能找到新增事项。
- 每周安排：能打开接下来三次安排预览。
- 权限联动：未选择查看时代记不可选；选择查看后代记可选，提醒不会自动选中。
- 编辑冲突：修改标题后切换冲突状态，比较弹层和返回后的编辑表单均保留本地标题。验证时发现状态切换丢失输入，已修复并复验。
- 首轮画板遍历后浏览器错误日志为空。

## 验证限制

- 320 / 390 / 430 CSS px 响应式视口验证尝试因浏览器工具超时未完成，不能视为已通过；临时视口设置已恢复。
- 邀请、恢复、交接、批量结果等提供本地示例或交互入口，本次没有逐条完成全流程自动验证。
- 原型使用虚构数据和固定演示日期，不连接 CloudBase，不发送真实邀请或通知。
- 没有修改小程序业务代码，没有进行微信开发者工具、真实云环境或真机验证。
- 设计文件不涉及构建或契约变更，因此没有运行业务 `npm run check`。

## 本地查看

在仓库根目录运行：

```sh
python3 -m http.server 4397 --bind 127.0.0.1 --directory design
```

打开 <http://127.0.0.1:4397/>。也可以直接打开 `index.html`，静态资源均为相对路径。

## 权限辨识补充验证

- 首页 DOM 与截图确认：可记录项显示「我可完成 / 我可代记」及完成圆框；只读项进入独立分区，显示眼睛图形、「仅查看」和查看记录说明。
- 点击只读「晚饭后散步」可进入详情，底部完成按钮数量为 0。
- 更新后的 JavaScript 语法检查通过。

### 移除可见权限标注

按最新反馈移除了逐项权限标签和只读说明，保留白底完成圆框、灰底眼睛图形与独立分区。浏览器检查确认权限标签数量为 0、只读眼睛图形为 1、只读完成圆框为 0；仅查看说明保留在无障碍名称中。此版本覆盖上面的可见标签设计。

## 取消个人事项的设计验证

- 首页筛选仅有全部家庭和具体家庭，快速新增仅有具体家庭；脚本不再包含个人事项入口或分支。
- 无家庭场景下首页新增按钮为 0；切换到编辑页后仍显示创建/加入引导，保存按钮为 0。
- 从该引导接受演示邀请后，能恢复新增入口并保存「周末整理客厅」，明天列表中显示其归属「我的小家」。
- 此调整仅涉及设计原型；真实业务和历史个人事项没有修改或迁移。
